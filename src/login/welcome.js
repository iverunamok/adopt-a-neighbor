const React, { Component } = require('react');
const { browserHistory, Link } = require('react-router');
const apiUrl = require('../config');

class Login


const objWithALongName = {
							innerObject : {
								name: "Kelsey",
								password: "Haxxxor"

							}
						 }


const name = objWithALongName.innerObject.name;
const password = objWithALongName.innerObject.password

console.log(name, password)

const { name, password } = objWithALongName.innerObject;
console.log(name, password)